---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.status.name }}
spec:
  ports:
    - protocol: {{ .Values.service.protocol | default "TCP" }}
      name: {{ .Values.service.status.name }}
      port: {{ .Values.service.status.port }}
  selector:
    app: {{ .Chart.Name }}
  type: NodePort
